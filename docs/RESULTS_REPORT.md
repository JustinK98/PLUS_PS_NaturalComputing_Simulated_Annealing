# Results Report

This report is intentionally reset for the new Basics benchmark suite.

The old `iris`-based report was archived at:

- `docs/archive/RESULTS_REPORT_legacy_iris.md`

Use the terminal suites in `docs/EXPERIMENTS.md` to regenerate the first valid result basis for:

- `two_moons`
- `concentric_circles`
- `crossing_spirals`

Ranking decisions must use mean validation loss across seeds. Test loss and test accuracy are final reporting metrics only.
